/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { WorsePipe } from './worse.pipe';

describe('WorsePipe', () => {
  it('create an instance', () => {
    let pipe = new WorsePipe();
    expect(pipe).toBeTruthy();
  });
});
